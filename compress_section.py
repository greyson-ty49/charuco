# -*- coding: utf-8 -*-
"""压缩论文4.3节"""

content = open('毕业论文4.md', 'r', encoding='utf-8').read()

# 新的压缩版4.3节
new_section = '''### 4.3 标定数据采集策略

相机标定的精度不仅取决于数学模型和优化算法，更依赖于标定数据的质量。高质量的标定数据应满足角点检测准确、标定板姿态多样、覆盖整个图像区域、畸变充分激发等条件。本节阐述标定数据采集的质量评估标准、多距离多角度采集策略以及数据质量控制方法。

#### 4.3.1 角点检测质量评估

对于$7 \\times 5$的ChArUco标定板，理论上存在24个棋盘格角点。考虑到PnP算法的数值稳定性要求，本系统设定最低角点数量阈值为8个，低于此阈值的图像将被拒绝使用。系统建立了四级质量分级标准：优秀级（≥20点，检测率≥83.3%）、良好级（16-19点）、可接受级（8-15点）和不合格级（<8点）。实验表明，当$N_{\\text{detected}} \\geq 12$时，位姿估计精度与全部角点可见时相差不大。

角点的空间分布均匀性同样影响标定精度。本系统采用角点分布熵评估均匀性，将标定板区域划分为$2 \\times 2$的网格，计算归一化分布概率和香农熵：

$$
p_k = \\frac{n_k}{\\sum_{i=1}^{4} n_i}, \\quad H = -\\sum_{k=1}^{4} p_k \\log_2 p_k
$$

熵值$H$的范围为$[0, 2]$，$H = 2$表示角点完全均匀分布，$H = 0$表示角点全部集中在单一子区域。实验确定的均匀性阈值为$H \\geq 1.5$，即要求至少3个子区域有角点分布。

在图像质量评估方面，系统采用拉普拉斯方差$\\text{Sharpness} = \\text{Var}(\\nabla^2 I)$评估清晰度，阈值设定为100；通过灰度标准差评估亮度均匀性，理想范围为30至70；通过统计极端灰度值像素比例评估曝光适当性，过曝或欠曝像素占比应低于5%。系统在采集过程中实时计算上述指标，当图像质量不满足要求时立即给出调整建议。

#### 4.3.2 多距离多角度采集策略

标定数据的空间分布多样性是决定相机参数估计精度与泛化能力的关键因素。本系统制定了包含多距离分层、多角度旋转以及全视场覆盖的综合采集策略。

在距离维度，依据机器人实际工作范围（0.5-2.0m），设定了覆盖0.4m至2.5m的扩展采集区间，划分为近距离层（0.4-0.8m，约束高阶径向畸变）、中距离层（0.8-1.5m，提供核心几何参数约束）和远距离层（1.5-2.5m，验证远场泛化性能）三个层次，每层至少采集3-5张不同姿态的图像。

**【图4-5 标定数据采集的三层距离分布示意图】**

**【表4-2 相机标定结果】**

**【表4-3 标定稳定性统计】**

在角度维度，俯仰角控制在$\\pm 30^\\circ$以内，偏航角扩展至$\\pm 40^\\circ$以充分激发横向畸变特征，滚转角在$\\pm 20^\\circ$范围内变化以区分切向畸变。系统强制要求数据集中包含至少30%的大角度倾斜图像（倾角$>20^\\circ$）。在视场位置覆盖上，系统将图像平面划分为$3 \\times 3$的九宫格区域，通过"中心定参数、四角约束畸变、边缘测离心误差"的空间采样逻辑，确保畸变模型在图像各区域都能得到有效校正。

**【图4-6 标定板在图像平面上的位姿分布要求】**

#### 4.3.3 标定数据质量控制体系

本系统建立了四级质量控制体系。在实时筛选阶段，系统强制要求角点数量$N_{\\text{detected}} \\geq 8$、空间分布熵$H \\geq 1.5$、拉普拉斯方差≥100，并设置重复性抑制策略，仅保留与已保存帧相比平移量$\\Delta t > 10$cm或旋转量$\\Delta R > 10^\\circ$的新增数据。

在离线评估阶段，系统验证有效图像总数是否达到15张以上，检查数据在近、中、远三个距离层的分布情况，并通过计算外参旋转矩阵的协方差评估姿态空间采样的多样性。

针对离群点剔除，系统引入基于中位数绝对偏差（MAD）的稳健统计方法：
$$
z_j = \\frac{e_j - \\text{median}(\\{e_j\\})}{1.4826 \\times \\text{MAD}}, \\quad \\text{其中 } \\text{MAD} = \\text{median}(|e_j - \\text{median}(\\{e_j\\})|)
$$
当某张图像的统计量$z_j > 3$时，系统将其判定为异常值并自动剔除。

标定完成后，系统通过多维度可视化报告辅助质量判断，包括去畸变前后对比图像、全视场误差散点图、单帧误差柱状图以及标定板三维位姿重构图。

**【图4-7 标定质量评估流程图】**

'''

# 找到需要替换的部分
start = content.find('### 4.3 标定数据采集策略')
end = content.find('### 4.4 实时位姿估计')

print(f'Start position: {start}')
print(f'End position: {end}')

if start != -1 and end != -1:
    # 替换
    new_content = content[:start] + new_section + content[end:]
    
    # 保存
    with open('毕业论文4.md', 'w', encoding='utf-8') as f:
        f.write(new_content)
        
    print('Done! Section compressed.')
    print(f'Old length: {end-start} chars')
    print(f'New length: {len(new_section)} chars')
else:
    print('ERROR: Could not find section markers!')
