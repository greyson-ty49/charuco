# 标定与实时检测流程说明

在标定阶段，我们用多张标定板照片，将每个角点的像素坐标 $(u, v)$ 和实际世界坐标 $(X_w, Y_w, Z_w)$ 输入标定算法，通过“重投影误差最小化”公式，自动求出相机内参矩阵 $K$ 和畸变参数。标定完成后，$K$ 已知。实时检测时，新拍一张标定板照片，检测到角点像素坐标 $(u, v)$，用畸变校正公式将其校正为真实像素坐标（undistorted points）。然后将这些校正后的像素坐标、已知的世界坐标 $(X_w, Y_w, Z_w)$ 以及 $K$ 一起输入PnP算法，PnP算法利用投影公式将三维点和二维点联系起来，解算出旋转矩阵 $R$ 和位移向量 $t$。最后，通过距离公式 $d = \sqrt{t_x^2 + t_y^2 + t_z^2}$，即可算出标定板到相机的空间距离。
